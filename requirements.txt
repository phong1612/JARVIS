Requirement:
Install Ollmaa, whisper, sounddevice, chromadb, psutil and any other dependencies required for the project. You can do this by running the following command:
pip install pypdf
pip install piper-tts
pip install ollama
pip install whisper
pip install chromadb
pip install ffmpeg